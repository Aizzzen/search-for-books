import React from 'react';

const TotalSchema = ({info}) => {
    return (
        <div className="d-flex justify-content-center align-items-center mt-3">
            {info}
        </div>
    )
}

export default TotalSchema
